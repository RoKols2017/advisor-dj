# 🚀 Быстрое развертывание Print Advisor в Docker

## ✅ Проверка готовности

Проект **готов к развертыванию** в Docker. Все необходимые компоненты на месте.

### Что уже есть:
- ✅ Docker Compose конфигурация (`docker-compose.yml`)
- ✅ Dockerfile для web-сервиса
- ✅ Dockerfile для watcher-сервиса
- ✅ Скрипт генерации .env файла (`scripts/generate_env.sh`)
- ✅ Health check endpoint (`/health/`)
- ✅ Smoke tests (`scripts/smoke.sh`)
- ✅ Production settings (`config/settings/production.py`)
- ✅ Nginx reverse proxy конфигурация

---

## 📋 Быстрый старт (5 шагов)

### Шаг 1: Генерация .env файла

```bash
cd /home/oitroot/project/advisor-dj

# Автоматическая генерация со всеми ключами
./scripts/generate_env.sh

# Или интерактивный режим (для настройки параметров)
./scripts/generate_env.sh --interactive

# Или с указанием разрешенных хостов
./scripts/generate_env.sh --allowed-hosts "192.168.1.100,localhost,127.0.0.1"
```

**Важно:** Скрипт автоматически генерирует:
- `SECRET_KEY` (Django)
- `POSTGRES_PASSWORD` (64 символа)
- `IMPORT_TOKEN` (для API импорта)
- `ALLOWED_HOSTS` (автоопределение или ручной ввод)

### Шаг 2: Создание каталогов для данных

```bash
# Создать каталоги для watcher
mkdir -p data/{watch,processed,quarantine}

# Установить права доступа (для записи из контейнера)
sudo chmod 777 data/{watch,processed,quarantine}
```

### Шаг 3: Сборка и запуск Docker-контейнеров

```bash
# Собрать образы и запустить все сервисы
make up-build
# или
docker compose up --build -d

# Проверить статус
make status
# или
docker compose ps
```

**Сервисы:**
- `advisor-web` - Django веб-приложение (порт 8000)
- `advisor-watcher` - демон мониторинга файлов
- `advisor-db` - PostgreSQL 15 (порт 5432)

### Шаг 4: Выполнение миграций БД

```bash
# Выполнить миграции
make migrate
# или
docker compose exec web python manage.py migrate

# Создать суперпользователя
docker compose exec web python manage.py createsuperuser
```

### Шаг 5: Проверка работоспособности

```bash
# Запустить smoke tests
make smoke
# или
./scripts/smoke.sh

# Проверить health check
curl http://localhost:8000/health/

# Проверить логи
make logs-web
# или
docker compose logs -f web
```

---

## 🌐 Настройка Nginx Reverse Proxy (опционально)

Если нужен единый вход через Nginx (порт 80/443):

### Шаг 1: Создать сеть для reverse proxy

```bash
# Один раз
make nginx-setup
# или
docker network create reverse-proxy-network
```

### Шаг 2: Запустить Nginx

```bash
make nginx-up
# или
docker compose -f docker-compose.proxy.yml up -d
```

### Шаг 3: Проверить доступ

```bash
# Через Nginx (порт 80)
curl http://localhost/

# Логи Nginx
make nginx-logs
```

**Примечание:** Для HTTPS нужно настроить SSL-сертификаты (см. `docs/NGINX_REVERSE_PROXY_IMPLEMENTATION.md`).

---

## 🔧 Полезные команды

### Управление сервисами

```bash
# Запустить все сервисы
make up

# Остановить все сервисы
make down

# Перезапустить все сервисы
make restart

# Статус сервисов
make status

# Логи всех сервисов
make logs

# Логи конкретного сервиса
make logs-web
make logs-watcher
make logs-db
```

### Работа с БД

```bash
# Выполнить миграции
make migrate

# Django shell
make shell

# Создать суперпользователя
docker compose exec web python manage.py createsuperuser
```

### Тестирование

```bash
# Запустить тесты
make test

# Smoke tests
make smoke

# Линтинг
make lint
```

### Очистка

```bash
# Остановить и удалить контейнеры и volumes
make clean
```

---

## 📁 Структура каталогов

```
advisor-dj/
├── data/                    # Каталоги для watcher (создать вручную)
│   ├── watch/              # Сюда класть файлы для импорта
│   ├── processed/          # Обработанные файлы
│   └── quarantine/         # Файлы с ошибками
├── .env                     # Переменные окружения (сгенерировать)
├── docker-compose.yml       # Основная конфигурация
├── docker-compose.proxy.yml # Nginx reverse proxy
├── Dockerfile               # Образ для web
├── Dockerfile.watcher       # Образ для watcher
└── scripts/
    ├── generate_env.sh     # Генератор .env
    └── smoke.sh            # Smoke tests
```

---

## ⚠️ Важные моменты

### 1. Переменные окружения

**Обязательно обновить в `.env`:**
- `ALLOWED_HOSTS` - IP адрес или домен сервера
- `SECRET_KEY` - сгенерирован автоматически
- `POSTGRES_PASSWORD` - сгенерирован автоматически

### 2. Права доступа

Watcher должен иметь права на запись в каталоги `data/`. Если возникают проблемы:

```bash
# Вариант 1: Полные права (проще, но менее безопасно)
sudo chmod 777 data/{watch,processed,quarantine}

# Вариант 2: Права для группы (безопаснее)
sudo chown -R 999:999 data/{watch,processed,quarantine}
sudo chmod 775 data/{watch,processed,quarantine}
```

### 3. Без интернета

Если сервер без интернета, нужно заранее:

```bash
# На машине с интернетом: собрать и сохранить образы
docker compose build
docker save advisor-dj-web:latest advisor-dj-watcher:latest postgres:15 -o advisor-dj-images.tar

# На целевом сервере: загрузить образы
docker load -i advisor-dj-images.tar
docker compose up -d
```

### 4. Автозапуск при перезагрузке

Все контейнеры имеют `restart: unless-stopped`, но для автозапуска при полной перезагрузке системы:

```bash
sudo cp advisor-dj.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable advisor-dj.service
sudo systemctl start advisor-dj.service
```

---

## 🔍 Проверка после развертывания

### Базовая проверка

1. ✅ Все контейнеры в статусе `healthy`
   ```bash
   docker compose ps
   ```

2. ✅ Health check возвращает 200
   ```bash
   curl http://localhost:8000/health/
   ```

3. ✅ Веб-интерфейс доступен
   ```bash
   curl http://localhost:8000/
   ```

4. ✅ Watcher обрабатывает файлы
   ```bash
   # Положить тестовый файл
   echo '[]' > data/watch/test.json
   # Проверить через несколько секунд
   ls data/processed/
   ```

### Функциональная проверка

1. ✅ Вход в систему через суперпользователя
2. ✅ Отображение дашборда (`/`)
3. ✅ Отображение дерева событий (`/tree/`)
4. ✅ Отображение статистики (`/statistics/`)
5. ✅ Импорт файлов через watcher

---

## 📚 Дополнительная документация

- **Подробный чеклист:** `docs/DEPLOYMENT_CHECKLIST.md`
- **Готовность к развертыванию:** `docs/DEPLOYMENT_READINESS.md`
- **Nginx reverse proxy:** `docs/NGINX_REVERSE_PROXY_IMPLEMENTATION.md`
- **Настройка watcher:** `docs/FILE_WATCHER_SETUP.md`
- **Руководство по эксплуатации:** `docs/RUNBOOK.md`

---

## 🆘 Решение проблем

### Проблема: Контейнеры не запускаются

```bash
# Проверить логи
docker compose logs

# Проверить конфигурацию
docker compose config

# Пересобрать образы
docker compose build --no-cache
```

### Проблема: Watcher не обрабатывает файлы

```bash
# Проверить права доступа
ls -la data/

# Проверить логи watcher
docker compose logs watcher

# Проверить, что каталоги смонтированы
docker compose exec watcher ls -la /app/data/
```

### Проблема: База данных не подключается

```bash
# Проверить статус БД
docker compose exec db pg_isready -U advisor

# Проверить переменные окружения
docker compose exec web env | grep DATABASE
```

---

## ✅ Итог

Проект **готов к развертыванию**. Минимальные действия:

1. `./scripts/generate_env.sh` - создать .env
2. `mkdir -p data/{watch,processed,quarantine} && chmod 777 data/*`
3. `docker compose up --build -d` - запустить
4. `docker compose exec web python manage.py migrate` - миграции
5. `docker compose exec web python manage.py createsuperuser` - создать админа

После этого приложение будет доступно на `http://localhost:8000` (или через Nginx на порту 80).
