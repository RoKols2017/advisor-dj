# Конфигурация для advisor приложения
# Этап A (разработка): HTTP only, server_name _ (работает с любым хостом/IP)

upstream advisor_backend {
    server advisor-web:8000;
}

server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;
    
    # Healthcheck endpoint (работает на HTTP для проверки доступности Nginx)
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
    
    # Лимиты
    client_max_body_size 10m;
    
    # Проксирование всех остальных запросов к Django
    location / {
        include /etc/nginx/snippets/proxy-common.conf;
        proxy_pass http://advisor_backend;
    }
}

# Этап B (production): Раскомментировать после настройки SSL
# 
# 1. Получить сертификаты от MS CA
# 2. Разместить в infrastructure/certs/server/
# 3. Раскомментировать блоки ниже
# 4. Раскомментировать порт 443 в docker-compose.proxy.yml
# 5. Обновить server_name на реальный домен
#
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name advisor.domain.local;
#     
#     # SSL сертификаты
#     ssl_certificate /etc/nginx/certs/server/advisor.domain.local.crt;
#     ssl_certificate_key /etc/nginx/certs/server/advisor.domain.local.key;
#     
#     # SSL настройки
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#     
#     # Security headers
#     include /etc/nginx/snippets/security-headers.conf;
#     
#     # Лимиты
#     client_max_body_size 10m;
#     
#     # Healthcheck endpoint (работает и на HTTPS)
#     location /health {
#         access_log off;
#         proxy_pass http://advisor_backend;
#         include /etc/nginx/snippets/proxy-common.conf;
#     }
#     
#     # Проксирование всех остальных запросов к Django
#     location / {
#         include /etc/nginx/snippets/proxy-common.conf;
#         proxy_pass http://advisor_backend;
#     }
# }
#
# # Редирект HTTP → HTTPS (раскомментировать вместе с HTTPS блоком)
# # server {
# #     listen 80;
# #     listen [::]:80;
# #     server_name advisor.domain.local;
# #     
# #     # Healthcheck endpoint без редиректа (для мониторинга)
# #     location /health {
# #         access_log off;
# #         return 200 "healthy\n";
# #         add_header Content-Type text/plain;
# #     }
# #     
# #     # Всё остальное — редирект на HTTPS
# #     location / {
# #         return 301 https://$host$request_uri;
# #     }
# # }

